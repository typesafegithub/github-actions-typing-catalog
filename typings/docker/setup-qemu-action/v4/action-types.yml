inputs:
  image:
    type: string
  cache-image:
    type: boolean
  reset:
    type: boolean
  platforms:
    type: list
    separator: ','
    list-item:
      type: string
outputs:
  platforms:
    type: list
    separator: ','
    list-item:
      type: string
