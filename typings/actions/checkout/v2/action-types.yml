# See https://github.com/typesafegithub/github-actions-typing
inputs:
  repository:
    type: string
  ref:
    type: string
  ssh-key:
    type: string
  ssh-known-hosts:
    type: string
  persist-credentials:
    type: boolean
  path:
    type: string
  clean:
    type: boolean
  lfs:
    type: boolean
  submodules:
    type: boolean
  set-safe-directory:
    type: boolean
